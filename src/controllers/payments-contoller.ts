import { Request, Response } from "express";

export async function getTicketPaymentInfo (req: Request, res:Response){
    const ticketId:number = Number(req)
}


export async function processNewPayment (req: Request, res:Response){

}